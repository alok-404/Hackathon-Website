{
  "name": "hackathon-backend",
  "version": "1.0.0",
  "description": "json-server backend for hackathon project",
 "scripts": {
  "start": "cross-env PORT=${PORT:-10000} json-server --watch db.json --port $PORT"
}
,
  "dependencies": {
    "cross-env": "^7.0.3",
    "json-server": "^1.0.0-beta.3"
  }
}
